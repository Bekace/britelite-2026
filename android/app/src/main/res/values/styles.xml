<resources>
    <!-- Updated theme name from Theme.PointerTV to Theme.XkreenPlayer -->
    <style name="Theme.XkreenPlayer" parent="android:Theme.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
