<resources>
    <!-- Changed theme parent to AppCompat theme to fix crash with AppCompatActivity -->
    <style name="Theme.XkreenPlayer" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
